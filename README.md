# Screenshot Heatmap

Text-to-image heatmap and detection comparison. Compare SigLIP GradCAM, CLIPSeg, GEM heatmaps with GroundingDINO and OWL-ViT bounding box detections.

## Setup (uv)

```bash
uv sync
```

## Run

```bash
uv run python app.py
```
